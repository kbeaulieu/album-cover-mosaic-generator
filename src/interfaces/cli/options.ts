export interface Options {
    listId: string;
    outputPath: string;
    backgroundColor: string;
    gradient: boolean;
    scaleToFit: boolean;
    overlay: string;
    overlayBlend: string;
    paddingSize: string;
    width: string;
    height: string;
}
